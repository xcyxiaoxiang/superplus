## Capabilities

### New Capabilities
- `<name>`: <brief description>

### Modified Capabilities
- `<existing-name>`: <what requirement is changing>

## Context

### Change Background
<!-- 1-2 sentences: what problem are we solving and why -->
**Base package**: `<package>`

### Current State
<!-- 1-2 sentences: what exists now and what's missing -->

### Constraints
<!-- 1-2 sentences: technical/business constraints and scope boundaries -->

### References
- **Design**: [Design doc](docs/designs/YYYY-MM-DD-<topic>-design.md)
- **Specs**: `docs/changes/<name>/1.specs/`

## Domain Context

<!-- Business domain background that subagents need to understand the WHY behind the WHAT -->

### Key Concepts
<!-- Define domain terms, acronyms, and business entities that appear in this change.
     Subagents must understand what things MEAN, not just their names. -->
- `<term>`: <business meaning, format constraints, where it comes from>
- `<term>`: <business meaning, format constraints, where it comes from>

### Business Flow Position
<!-- Where does this change sit in the larger business pipeline?
     Subagents need to understand "I am step N of M" to avoid breaking upstream/downstream assumptions. -->
This change implements [which step] of [business pipeline name]:

  [ASCII diagram or step list showing the full pipeline with current change highlighted]

### Data Semantics
<!-- Key data entities, their relationships, and field-level semantics.
     This prevents subagents from misinterpreting field purposes or breaking implicit contracts. -->
- `<entity/field>`: <business meaning, valid values, source/destination>
- `<entity/field>`: <business meaning, valid values, source/destination>

## Architecture

<!-- ASCII architecture diagram + component interface signatures + data flow direction.
     Describe component responsibilities, not just connections.
     Subagents need to understand "what each component does" and "how they talk to each other". -->

[ASCII diagram]

### Component Responsibilities
<!-- Brief (1-2 sentences) description of what EACH component does.
     This prevents subagents from putting logic in the wrong component. -->
- `<Component A>`: <what it does, what it owns>
- `<Component B>`: <what it does, what it owns>

### Key Interfaces
<!-- Precise interface/method signatures at component boundaries.
     Include parameter types, return types, and semantic meaning. -->
- `<Component A>` → `<Component B>`: `<methodName>(<paramTypes>)` → `<ReturnType>` — <semantic meaning>

## Reference Code

<!-- Concrete code snippets that subagents should mimic.
     DO NOT describe patterns in prose — show the actual code from this project.
     Subagents learn by example, not by description. -->

<!-- Common pattern types:
     - ServiceImpl Error Handling
     - DTO Assembly Pattern
     - Mapper Interface Signature
     - Configuration Injection
     - Logging at Boundaries
     - Exception Translation
     - Repository Query Pattern
     - Validator Implementation
-->

### [Pattern Name]
<!-- Extract from existing codebase. Label what pattern this demonstrates. -->
```<language>
// Real code from this project showing the pattern
```

### [Pattern Name]
```<language>
// Real code from this project showing the pattern
```

## Implementation Order

<!-- Dependency topology + parallel markers.
     Start with independent leaf components, end with integration/glue code. -->

| Order | Component | Depends On | Parallel? |
|-------|-----------|------------|-----------|
| 1 | `<Component A>` | — | ✅ parallel with B |
| 2 | `<Component B>` | — | ✅ parallel with A |
| 3 | `<Component C>` | A, B | — |

## Cross-Cutting Concerns

<!-- Error handling, logging, security, config patterns, coding conventions.
     Each sub-section must contain enough detail for a subagent to follow without guessing. -->

### Error Handling
- Pattern: <specific pattern name and behavior>
- Code reference: <"See Reference Code section"> or inline example

### Logging
- Framework: <SLF4J / Log4j / Winston / etc.>
- Levels: <what situation → what level>
- Required log points: <entry/exit, error, business validation>

### Configuration
- Pattern: <@Value / @ConfigurationProperties / env / etc.>
- Examples: <key names and injection style>

### Coding Conventions
<!-- Project-specific coding norms not covered by linters/formatters -->
- <convention 1>
- <convention 2>

## Configuration

<!-- Configuration items table. If few items, merge into Cross-Cutting Concerns. -->

| Key | Type | Default | Required | Description |
|-----|------|---------|----------|-------------|
| `<key>` | `<string/number/boolean>` | `<value>` | ✅/❌ | <what it controls> |

## Testing Strategy

<!-- Test approach, framework, run commands.
     Per-component differences if applicable. -->

### Test Framework & Commands
- Framework: <JUnit 5 / Jest / Pytest / Go test / etc.>
- Run command: `<exact command>`
- Coverage tool: <if any>

### Mocking Strategy
- What to mock: <external services, databases, file I/O>
- What NOT to mock: <pure functions, utilities>
- Fixture location: <if applicable>

### Per-Component Notes
<!-- Component-specific testing considerations. Only include if components have significantly different testing requirements. -->
- `<Component A>`: <special testing note>
- `<Component B>`: <special testing note>

---

## Tasks

### 1. <!-- Component Name -->

**Files:**
- Create: `src/path/to/file.ts`
- Modify: `src/path/to/existing.ts`
- Test: `tests/path/to/test.ts`

- [ ] **1.1 Write the failing test**

```typescript
test('specific behavior', () => {
  const result = functionUnderTest(input);
  expect(result).toBe(expected);
});
```

- [ ] **1.2 Run test to verify it fails**

Run: `npm test -- tests/path/test.ts`
Expected: FAIL — "function not defined"

- [ ] **1.3 Write minimal implementation**

```typescript
export function functionUnderTest(input: Type): ReturnType {
  return expected;
}
```

- [ ] **1.4 Run test to verify it passes**

Run: `npm test -- tests/path/test.ts`
Expected: PASS

### 2. <!-- Component Name -->

**Files:**
- Create: `...`
- Modify: `...`
- Test: `...`

- [ ] **2.1 ...**
